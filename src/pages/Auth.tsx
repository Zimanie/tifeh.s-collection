import { Link, useLocation } from 'react-router-dom'
import { Button } from '../components/Button'

export function Login() { return <AuthForm mode="login" /> }
export function Register() { return <AuthForm mode="register" /> }
function AuthForm({ mode }: { mode: 'login' | 'register' }) { const isLogin = mode === 'login'; const location = useLocation(); return <main className="mx-auto flex min-h-[70vh] max-w-md items-center px-5 py-16"><div className="w-full"><p className="mb-3 text-center text-[10px] uppercase tracking-[0.25em] text-[#C5A059]">Tifeh.s private client</p><h1 className="text-center font-serif text-5xl font-light">{isLogin ? 'Welcome back' : 'Create an account'}</h1><p className="mt-4 text-center text-xs leading-6 text-[#706F6C]">{isLogin ? 'Sign in to access your saved pieces and orders.' : 'Join the house for a more personal way to shop.'}</p><form className="mt-10 space-y-6"><input required type="email" placeholder="Email address" className="w-full border-b border-[#D6D2C9] bg-transparent py-3 text-sm outline-none" /><input required type="password" placeholder="Password" className="w-full border-b border-[#D6D2C9] bg-transparent py-3 text-sm outline-none" />{!isLogin && <input required type="password" placeholder="Confirm password" className="w-full border-b border-[#D6D2C9] bg-transparent py-3 text-sm outline-none" />}<Button type="submit" className="w-full">{isLogin ? 'Sign in' : 'Create account'}</Button></form><p className="mt-8 text-center text-xs text-[#706F6C]">{isLogin ? 'New to Tifeh.s?' : 'Already a client?'} <Link to={isLogin ? '/register' : '/login'} state={{ from: location.pathname }} className="text-[#1A1A1A] underline underline-offset-4">{isLogin ? 'Create an account' : 'Sign in'}</Link></p></div></main> }
